typeof ใช้ตรวจสอบชนิดข้อมูลของตัวแปรว่าคือ string หรือไม่
trim() ใช้ตัดช่องว่างด้านหน้าและด้านหลังข้อความออก
toLowerCase() ใช้แปลงข้อความทั้งหมดให้เป็นตัวพิมพ์เล็ก
match() + Regular Expression (RegExp)ใช้ค้นหาและดึงคำออกมาจากข้อความ Regex /\p{L}[\p{L}\p{N}'-]*/gu กำหนดรูปแบบคำที่รองรับอักษร ตัวเลข และเครื่องหมาย ', -

ตัวดำเนินการ || (OR) ใช้กำหนดค่าเริ่มต้นเป็น [] (อาร์เรย์ว่าง) ในกรณีที่ match() คืนค่า null
for...of loop ใช้วนลูปผ่านแต่ละคำในอาร์เรย์ tokens
Nullish coalescing operator ?? ใช้กำหนดค่าเริ่มต้นเป็น 0 ถ้า key ยังไม่มีในอ็อบเจ็กต์ freq
การเข้าถึง/กำหนดค่าอ็อบเจ็กต์ ใช้ freq[tk] = ... เพื่อเก็บจำนวนครั้งที่คำปรากฏ
return ใช้คืนผลลัพธ์เป็นอ็อบเจ็กต์ { คำ: จำนวนครั้ง }